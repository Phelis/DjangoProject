import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

const rootElement = document.getElementById('react-app');

ReactDOM.render(
    <App name="Taylor" age = "123" para = "111111"/>, rootElement
);
